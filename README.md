# mcp-forge
mcp_forge is a CLI tool that scaffolds a fully functional Model Context Protocol (MCP) server from any OpenAPI 3.x specification. By parsing an OpenAPI spec (from a URL or local file), it auto-generates tool definitions, request handlers, authentication boilerplate, and a ready-to-run MCP server in either Python or Node.js. Inspired by the growing 
